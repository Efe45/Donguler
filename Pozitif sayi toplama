#include <stdio.h>

int main(void) {

  int sayi = 1,toplam = 0;


  while(sayi > 0)
  {
  
  printf("Sayı");
  scanf("%d", &sayi);
    if (sayi <= 0)
    {
      break;
    }
    toplam += sayi;

  }
  printf("toplam: %d ",toplam);

}
